apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cze-repo-repo
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: production
  source:
    repoURL: https://github.com/cze-dk/crossplane-github
    targetRevision: HEAD
    path: chart
    helm:
      releaseName: cze-repo
      parameters:
        - name: github.repository
          value: cze-repo
        - name: application.name
          value: cze-repo
        - name: application.host
          value: cze-repo.acme.com
        - name: hyperscaler
          value: aws
  destination:
    server: https://kubernetes.default.svc
    namespace: infra
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
      allowEmpty: true
